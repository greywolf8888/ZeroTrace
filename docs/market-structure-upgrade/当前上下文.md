# 监管取证级盘面结构升级｜当前上下文

**记录时间：** 2026-08-19  
**工作 HEAD（升级前基线）：** `13234a42ec85a009134691a3cd75a346bac695f5`  
**工具链：** Node v24.11.1 / npm 11.6.2（与 `engines` 及 `.nvmrc` 一致）  
**分支：** `main`（相对 `origin/main` 在基线时 ahead 12；升级提交叠加于此后）

## 现场保护

未提交用户文件（不得覆盖、删除或纳入提交）：

- `ZeroTrace_监管取证级盘面结构分析_全量升级提示词包/`
- `ZeroTrace_监管取证级盘面结构分析_全量升级提示词包.zip`
- `output/`

升级代码与文档已写入工作树，按逻辑边界提交。最终交付见 `docs/market-structure-upgrade/最终交付.md`。

## 实际审计摘要（非文件名猜测）

已打开并核对的巨型组合层：

| 文件                            | 行数 | 事实                                                |
| ------------------------------- | ---: | --------------------------------------------------- |
| `apps/web/src/App.tsx`          | 9285 | 8 个英文工作区、无 i18n、直接调用手写 `api.ts`      |
| `packages/schemas/src/index.ts` | 8923 | 单文件 Zod 契约；Knowledge/Evidence/Snapshot 已存在 |
| `apps/api/src/app.ts`           | 7772 | Fastify 路由全部内联；`/api/v1/*`；无认证中间件     |
| `apps/web/src/api.ts`           | 3113 | 手写 interface，不引用 `@zerotrace/schemas`         |

已实现且必须通过兼容层保留：Evidence Ledger、Provider 安全、Reorg/数据质量、ControlCampaign、Funding/Settlement、Claim Audit、Token Flow、Cluster Position、Behavior、Entity、V2 RV/`simulateExitRace`、PostgreSQL 迁移至 `036`。

缺失且必须新建：`ForensicFinding`/`ReportEnvelope`/`CoverageVector`、供应立方体、角色互斥策略、疑似关联隐匿仓与散户正证据、Economic Lot、活动级双重记账、全盘共享流动性退出、中文工作站、API v2、统一 `forensic_reports`。
