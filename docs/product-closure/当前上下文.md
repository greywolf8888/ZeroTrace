# ZeroTrace 产品闭环当前上下文

更新时间：2026-08-21（Asia/Singapore）

## 权威边界

- 仓库 `AGENTS.md`、`ARCHITECTURE.md`、ZeroTrace Master Prompt 与终局盘面分析提示词包优先于聊天历史。
- 本轮产品焦点是“任意受支持 BSC Token 的案件型盘面分析与持续监控 V1”，但 EVM、Bitcoin、Solana、实体解析、Launchpad Intelligence、Realizable Value、Evidence、Scenario 与 UI 仍是一级架构域。
- 系统只读；禁止私钥托管、签名、审批、Swap、交易广播和自动资金移动。
- Unknown、Unavailable、Stale、Provider Down 必须分开，均不得折算为数值 0。
- 只有 G0–G14 全部在当前 SHA 有可复核 Evidence 时才允许合并或发布。

## Git 上下文

- 基线受保护分支：`main`
- 基线 SHA：`68ebfd9ae92305dd47bbc3175af07bda1a172c61`
- 当前唯一工作分支：`agent/terminal-market-structure-v1`
- 将最新 `main` 纳入工作分支的合并提交：`4436f7765c2602671cf8210bad47d360ec351281`
- 远端同名旧工作分支已不存在；未 force-push，未改写历史，未触碰用户未跟踪资产与 `output/`。

## 当前真实状态

- 基线 TypeScript 单元、集成、构建、许可证、审计和 Rust workspace 测试通过。
- 基线 Playwright 在 Windows 自动使用 12 workers 时出现 33 个 context teardown 超时；限制 Windows 默认 2 workers 后，桌面项目 21/21、全量 42/42 连续通过。
- Cannot-Link 传递冲突、PELT/BOCPD 名义错配、Unknown→0、V3 虚拟储备和多场所贪心“伪精确”已建立反例并修复或明确降级。
- 正式 Token API 具备“无持久队列则 fail closed”，但当前 HTTP handler 仍同步执行物化，独立 durable worker 未成为唯一主链。
- Desktop 仍是依赖源码、Node/npm 和固定开发端口的 workspace-linked launcher，不是可安装产品。
- 当前一级导航仍以技术模块展开；真实 Tauri、任意 Token 全生命周期、全局精确 V3/共享池 RV、50 Token 人工语料、签名安装包和 24h Soak 尚未闭合。

## 下一项代码任务

把正式 Token 请求改为持久任务入队和后台 worker 执行，移除生产 worker 中按 Token 硬编码 creation transaction 的路径，并让 UI 展示 QUEUED/RUNNING/PARTIAL 状态。
