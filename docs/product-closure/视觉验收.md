# 视觉验收

当前状态：`PARTIAL`。

已完成：

- Chromium desktop/mobile 42/42，窄屏根节点无横向溢出。
- Windows 自启动包装器抽样 2/2。
- 隔离安装后的真实 Tauri 原生窗口人工检查：工作台/查询、案件、监控与告警、数据源与系统四个一级入口均可切换；真实 API 返回 `DEGRADED`，没有被改写为 UP/0。
- 原公司图标通过确定性 SVG 包装生成 Tauri 图标；AI 生成候选因伪透明棋盘格且无 alpha 被拒绝，未进入交付。

未完成：运行中/READY 案件、Campaign、Entity、Evidence Drawer、告警详情、Provider Setup、OFFLINE/SOURCE_CONFLICT/FAILED 等 11 类绑定 SHA 的黄金截图集；因此不能标为 Perceptual PASS。

中文扫描脚本只扫描 6 个指定文件。普通四入口可见页面已人工核对为简体中文（协议名和标准缩写保留），但 Developer 区遗留英文不应被“0 命中”掩盖。
