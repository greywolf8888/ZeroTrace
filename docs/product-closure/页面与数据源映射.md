# 页面与数据源映射

| 一级入口      | 数据源                                                                     | 当前状态            | 产品边界                                                                     |
| ------------- | -------------------------------------------------------------------------- | ------------------- | ---------------------------------------------------------------------------- |
| 工作台 / 查询 | health、capabilities、platform/provider、`POST /api/v2/tokens/.../analyze` | CONNECTED / PARTIAL | 无默认示例 Token；最近案件/任务/告警没有持久数据时显示未加载/Unknown，不补 0 |
| 案件          | job、report、investigation、Evidence/Scenario APIs                         | PARTIAL             | 正式创建返回 202；轮询、取消、重试可用；完整任意 Token stage 闭环未完成      |
| 监控与告警    | capture schedule、Campaign/alert APIs                                      | PARTIAL             | 展示已有持久能力；24h、reorg/retract/restart 未过                            |
| 数据源与系统  | health、provider、storage、capabilities                                    | CONNECTED / PARTIAL | 区分 UP/DEGRADED/UNCONFIGURED/UNKNOWN；系统管理与数据健康作为二级入口        |

Campaign、供应、实体、资金、利润、情景、Evidence、Bitcoin、Solana、Launchpad 等一级架构域仍保留在代码和 Developer 折叠区，不再挤占普通用户一级导航。

Desktop 已从 workspace launcher 替换为真实 Tauri production Web + 打包 API sidecar；动态 loopback、会话 token 和 AppData 存储根已接。本机安装与原生窗口通过不等于签名清洁机验收。

普通用户卡片应显示来源、区块覆盖、Snapshot/Finality、更新时间、Unknown/Partial 原因和 Evidence 入口；无法回答的内容继续保持 Developer/PARTIAL。
